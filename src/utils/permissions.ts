import { UserRole, Resource, Action, Permission } from '../types';

// Ce fichier sert désormais principalement à définir les types et potentiellement à réexporter le hook.
// La logique de permission et la carte sont gérées par le PermissionsProvider et le hook usePermissions.

export type { UserRole, Resource, Action, Permission };